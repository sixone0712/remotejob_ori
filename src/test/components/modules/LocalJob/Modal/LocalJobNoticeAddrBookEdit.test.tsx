import { shallow } from 'enzyme';
import React from 'react';
import LocalJobNoticeAddrBookEdit from '../../../../../components/modules/LocalJob/Modal/LocalJobNoticeAddrBookEdit';

describe('renders the component', () => {
  it('renders correctly', () => {
    const component = shallow(<LocalJobNoticeAddrBookEdit />);
  });
});
